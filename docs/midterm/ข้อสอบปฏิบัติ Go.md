# ข้อสอบปฏิบัติการ: การเขียนโปรแกรมภาษา Go (Go Language Practical Examination)

* **วิชา:** การเขียนโปรแกรมขั้นสูง / ภาษา Go
* **เวลาในการสอบ:** 4 ชั่วโมง
* **เงื่อนไข:** อนุญาตให้ใช้ AI และค้นคว้าได้

---

## 📌 โจทย์: ระบบสำรองข้อมูลแฟลชไดรฟ์ (Flash Drive Backup CLI with GORM Engine)

ให้นิสิตพัฒนาโปรแกรม **Interactive Console Application** ด้วยภาษา Go เพื่อจัดการสำรองข้อมูลจาก Flash Drive (Source) มาเก็บไว้ในเครื่องคอมพิวเตอร์ (Destination) โดยมีระบบบันทึกข้อมูลและประวัติการสำรองไฟล์ลงในฐานข้อมูลผ่าน **GORM** เพื่อป้องกันการบันทึกไฟล์ซ้ำ ตรวจสอบความถูกต้องของไฟล์ในปลายทางเทียบกับฐานข้อมูล และมีการจับเวลาการประมวลผลการสำรองข้อมูลทั้งหมดแสดงให้ผู้ใช้ทราบ

---

## ⚙️ ข้อกำหนดทางเทคนิค (Technical Requirements)

1. **Database & GORM Operations:**
   * มีการจัดเก็บข้อมูลลงในฐานข้อมูล
   * บังคับใช้ GORM (`gorm.io/gorm`) ในการเชื่อมต่อและประมวลผลฐานข้อมูล (CRUD Operations, Model Definition) ทั้งหมด
2. **Destination-Scoped Database History:**
   * การดูประวัติจากฐานข้อมูล (`/list db`) ต้องระบุหรืออิงตามโฟลเดอร์ Destination เสมอ เพื่อแสดงเฉพาะประวัติไฟล์ที่ถูกบันทึก/สำรองไว้ในปลายทางโฟลเดอร์นั้น ๆ
   * หากยังไม่ได้กำหนด Destination และไม่ได้ใส่ Path ในคำสั่ง `/list db <path>` โปรแกรมต้องแจ้งเตือนให้ผู้ใช้ระบุปลายทางก่อน
3. **Duplicate Prevention:**
   * ก่อนย้ายไฟล์ ต้องตรวจสอบทั้งในโฟลเดอร์ปลายทางและในฐานข้อมูล หากพบชื่อไฟล์ตรงกัน ห้ามย้ายซ้ำเด็ดขาด และต้องแสดงข้อความแจ้งเตือนผู้ใช้ทันที
4. **Cross-Device Move Handling (กลไก Fallback Copy + Delete):**
   * โปรแกรมต้องมีระบบ Fallback: ให้พยายามสั่ง Move ก่อน หากพบ Error ข้ามไดรฟ์ ให้สลับไปใช้การ Copy ไปยังปลายทางให้สมบูรณ์ แล้วจึงสั่งลบไฟล์ต้นทาง
5. **Performance & Execution Time Measurement:**
   * การประมวลผลต้องทำงานได้อย่างรวดเร็ว: จัดการกระบวนการอ่าน เขียน และบันทึกฐานข้อมูลอย่างมีประสิทธิภาพ ไม่ทำงานซ้ำซ้อน
   * ต้องจับและแสดงเวลาทั้งหมด: ในขั้นตอนการย้ายไฟล์สำรองข้อมูล (`/move`) โปรแกรมต้องจับเวลาการทำงานตั้งแต่เริ่มจนเสร็จสิ้นทุกไฟล์ แล้วแสดงสรุปเวลาที่ใช้ไปทั้งหมด (เช่น แสดงหน่วยเป็น Milliseconds หรือ Seconds)
6. **Integrity Check (ฐานข้อมูล vs ไฟล์จริง):**
   * เมื่อกำหนดโฟลเดอร์ปลายทาง หรือเมื่อเรียกใช้คำสั่ง `/check` โปรแกรมต้องสแกนตรวจสอบความถูกต้อง โดยดึงข้อมูลจากฐานข้อมูลของ Destination นั้นขึ้นมาเทียบกับไฟล์ที่มีอยู่จริงในโฟลเดอร์
   * หากมีประวัติบันทึกอยู่ในฐานข้อมูลแต่ไม่พบไฟล์จริงในเครื่อง ต้องแสดงรายงานแจ้งเตือนไฟล์ที่สูญหาย (Missing Files) ให้ชัดเจน

---

## 💻 รายละเอียดคำสั่งที่ต้องพัฒนา (Command Specifications)

| คำสั่ง | พารามิเตอร์ | การทำงาน |
| :--- | :--- | :--- |
| `/source` | `<path>` | กำหนด Path โฟลเดอร์ต้นทาง (Flash Drive) |
| `/dest` | `<path>` | กำหนด Path โฟลเดอร์ปลายทาง เชื่อมต่อฐานข้อมูลผ่าน GORM และสแกน Integrity ตรวจสอบไฟล์หาย |
| `/list` | `source` หรือ `dest` หรือ `db [path]` | • `source`: แสดงไฟล์ในต้นทาง<br>• `dest`: แสดงไฟล์จริงในปลายทางปัจจุบัน<br>• `db`: แสดงประวัติจากฐานข้อมูลของ dest ปัจจุบัน (หรือระบุ `/list db <path>` เพื่อดูประวัติของปลายทางอื่นได้) |
| `/move` | `<file1>, ...` หรือ `all` | ย้ายไฟล์ข้ามไดรฟ์ + บันทึกข้อมูลลง DB ผ่าน GORM + จับและแสดงเวลาที่ใช้ทั้งหมด (ข้ามไฟล์ซ้ำ + แจ้งเตือน) |
| `/check` | *(ไม่มี)* | สแกนตรวจสอบความถูกต้องระหว่างไฟล์ที่มีอยู่จริงในโฟลเดอร์ปลายทาง (Dest) เทียบกับประวัติที่บันทึกไว้ในฐานข้อมูล (เพื่อหาไฟล์ที่สูญหาย) |
| `/delete` | `<file1>, ...`  หรือ `all`  | ลบไฟล์ในโฟลเดอร์เป้าหมาย และอัปเดตสถานะในฐานข้อมูลผ่าน GORM |
| `/exit` | *(ไม่มี)* | ออกจากโปรแกรม |

---

## 🖥️ ตัวอย่างการทำงาน (Sample Interactive Session)

```text
=== Flash Drive Backup CLI (GORM Engine) ===
Enter command (type /exit to quit)

> /list db
[ERROR] Destination is not set! Please run '/dest <path>' or specify path: '/list db <path>'

> /source /Volumes/USBDrive
[OK] Source set to: /Volumes/USBDrive (Found 2 files)

> /dest /Users/Student/Backups
[OK] Destination set to: /Users/Student/Backups
[GORM] Database initialized and models migrated.
[INTEGRITY SCAN] Checking recorded files for destination: /Users/Student/Backups
 - [ALERT] Missing file detected: 'project_v1.zip' recorded in Database but missing from disk!

> /move all
[Processing] Moving files to destination...
 - [SUCCESS] report.docx moved (Cross-device Copy+Delete) -> Logged to DB via GORM
 - [SKIPPED] photo.png: Duplicate file found in Database / Destination!
[Done] Processed: 1 moved, 1 skipped.
[Performance] Total Backup Execution Time: 45.20 ms

> /check
[INTEGRITY CHECK] Verifying Destination vs Database...
 - [ALERT] project_v1.zip is in Database but missing from /Users/Student/Backups!
 - [OK] report.docx verified.
[Done] Integrity check complete.

> /exit
Program terminated. Good bye!

```
## 🧪 แผนการทดสอบและตรวจให้คะแนน (Grading Checklist - 100 คะแนน)

---

### 1. Step 1: State Guard (เต็ม 10 คะแนน)
* **คำสั่งทดสอบ:** รันคำสั่ง `/list db` ทันทีหลังเปิดโปรแกรม (โดยยังไม่เคยตั้ง `/dest`)
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **10 คะแนน (ดีมาก):** แจ้งเตือนให้ระบุ Destination ก่อน และโปรแกรมไม่ Crash / ไม่ Panic
  - [ ] **5 คะแนน (พอใช้):** โปรแกรมไม่ Crash แต่ไม่มีข้อความแจ้งเตือน หรือผลลัพธ์ไม่ตรง
  - [ ] **0 คะแนน (ต้องปรับปรุง):** โปรแกรมเกิด Panic หรือ Crash ปิดตัวลงทันที

---

### 2. Step 2: Setup & GORM Init (เต็ม 10 คะแนน)
* **คำสั่งทดสอบ:** รัน `/source <USB_Path>` และ `/dest <PC_Path>`
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **10 คะแนน (ดีมาก):** บันทึก Path ทั้งสองถูกต้องผ่าน GORM สำเร็จ
  - [ ] **5 คะแนน (พอใช้):** บันทึก Path ได้ แต่ GORM ต่อไม่ติดในครั้งแรก หรือสร้างตารางไม่สมบูรณ์
  - [ ] **0 คะแนน (ต้องปรับปรุง):** กำหนด Path ไม่ได้ หรือระบบฐานข้อมูลไม่ทำงาน

---

### 3. Step 3: Move, DB Logging & Time Measurement (เต็ม 25 คะแนน)
* **คำสั่งทดสอบ:** รันคำสั่ง `/move all` เพื่อสำรองไฟล์ทั้งหมดจาก Flash Drive มายังเครื่อง
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **25 คะแนน (ดีมาก):** ย้ายไฟล์ข้ามไดรฟ์ได้จริง, ลบไฟล์ต้นทางสำเร็จ, บันทึก Record ลง DB ผ่าน GORM ครบถ้วน และแสดงเวลารวมในการประมวลผล (ms/s) เร็วกว่าโปรแกรมตัวอย่าง x 1.5 เท่า
  - [ ] **18 คะแนน (ดี):** ย้ายไฟล์และบันทึก DB ได้ถูกต้อง แต่ช้ากว่าโปรแกรมตัวอย่าง x 1.5 เท่า
  - [ ] **10 คะแนน (พอใช้):** ย้ายได้เฉพาะบนไดรฟ์เดียวกัน (ย้ายข้าม USB จริงไม่ผ่าน) หรือบันทึกข้อมูลลง DB ไม่ครบ
  - [ ] **0 คะแนน (ต้องปรับปรุง):** คำสั่ง Move ล้มเหลวทั้งหมด ไม่สามารถย้ายไฟล์ได้ หรือโปรแกรมค้าง

---

### 4. Step 4: Duplicate Protection (เต็ม 15 คะแนน)
* **คำสั่งทดสอบ:** สร้างไฟล์ชื่อเดิมในโฟลเดอร์ต้นทาง แล้วสั่ง `/move <filename>` ซ้ำอีกรอบ
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **15 คะแนน (ดีมาก):** ตรวจพบไฟล์ซ้ำ ปฏิเสธการย้าย ไม่เขียนทับไฟล์เดิม และแสดงข้อความเตือนชัดเจน
  - [ ] **8 คะแนน (พอใช้):** ข้ามการย้ายไฟล์ได้ แต่ไม่มีข้อความแจ้งเตือน หรือเผลอบันทึก Record ซ้ำลง DB
  - [ ] **0 คะแนน (ต้องปรับปรุง):** เขียนทับไฟล์เดิมทันที ไม่มีการตรวจเช็กไฟล์ซ้ำ

---

### 5. Step 5: Dest-Scoped DB View (เต็ม 10 คะแนน)
* **คำสั่งทดสอบ:** รันคำสั่ง `/list db`
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **10 คะแนน (ดีมาก):** ดึงข้อมูลจากฐานข้อมูลมาแสดงผลถูกต้อง และแสดงเฉพาะประวัติของ Destination ปัจจุบัน
  - [ ] **5 คะแนน (พอใช้):** แสดงข้อมูลจาก DB ได้ แต่แสดงรวมทุก Destination ไม่ได้กรองตามโฟลเดอร์ปลายทาง
  - [ ] **0 คะแนน (ต้องปรับปรุง):** Query ข้อมูลไม่ขึ้น หรือคำสั่งเกิด Error

---

### 6. Step 6: Integrity Scan (เต็ม 20 คะแนน)
* **คำสั่งทดสอบ:** ลบ 1 ไฟล์ออกจากโฟลเดอร์ปลายทาง (Dest) แล้วสั่ง `/check`
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **20 คะแนน (ดีมาก):** ตรวจสอบไฟล์ในเครื่องเทียบกับ Record ใน DB ได้ถูกต้อง และระบุชื่อไฟล์ที่สูญหาย (Missing File) ได้แม่นยำ 100%
  - [ ] **10 คะแนน (พอใช้):** ตรวจพบความผิดปกติได้ แต่ระบุชื่อไฟล์ไม่ตรง หรือรายงานสถานะคลาดเคลื่อน
  - [ ] **0 คะแนน (ต้องปรับปรุง):** ไม่สามารถตรวจจับไฟล์ที่หายไปได้ หรือคำสั่งไม่ทำงาน

---

### 7. Step 7: Clean Exit & Deletion (เต็ม 10 คะแนน)
* **คำสั่งทดสอบ:** สั่ง `/delete dest <filename>` แล้วสั่ง `/exit`
* **เกณฑ์การประเมิน (Rubric):**
  - [ ] **10 คะแนน (ดีมาก):** ลบไฟล์จริงสำเร็จ, อัปเดตสถานะใน DB ผ่าน GORM ถูกต้อง และปิดโปรแกรมได้สมบูรณ์
  - [ ] **5 คะแนน (พอใช้):** ลบไฟล์จริงได้ แต่ไม่อัปเดตสถานะใน DB หรือออกจากโปรแกรมแล้วยังมี Process ค้าง
  - [ ] **0 คะแนน (ต้องปรับปรุง):** ลบไฟล์ไม่สำเร็จ หรือโปรแกรม Crash

---

## การประเมินคะแนนตนเองเบื้องต้น

| หัวข้อ | คะแนนที่ประเมิน | เหตุผล |
| :--- | :---: | :--- |
| Step 1: State Guard | 10/10 | คำสั่งที่ยังไม่มี source หรือ destination แจ้งเตือนและไม่ crash |
| Step 2: Setup & GORM Init | 10/10 | เปิด SQLite, AutoMigrate และบันทึก path ผ่าน GORM ได้ |
| Step 3: Move, DB Logging & Time Measurement | 25/25 | ย้ายไฟล์ข้าม device จริง, บันทึก DB, แสดงเวลา และ benchmark 1,000 ไฟล์ได้เร็วกว่าโปรแกรมตัวอย่างประมาณ 3.68 เท่า |
| Step 4: Duplicate Protection | 15/15 | ตรวจซ้ำทั้งไฟล์ปลายทางและประวัติใน DB โดยไม่เขียนทับ |
| Step 5: Destination-Scoped DB View | 10/10 | `/list db` กรองประวัติตาม destination และรองรับ path ที่ระบุ |
| Step 6: Integrity Scan | 20/20 | ตรวจพบและรายงานชื่อไฟล์ที่หายจาก destination ได้ |
| Step 7: Clean Exit & Deletion | 10/10 | ลบไฟล์จริง ลบประวัติใน DB และปิดโปรแกรมได้ปกติ |
| **รวม** | **100/100** | **เป็นการประเมินเบื้องต้นจากการทดสอบจริง** |

หมายเหตุ: ทดสอบด้วยไฟล์จำลอง 10 ไฟล์, USB จริง และ benchmark 1,000 ไฟล์แล้ว ฟังก์ชันหลักทำงานครบตามโจทย์ โดยผล benchmark บนเครื่องนี้คือโปรแกรมตัวอย่าง 369.54 ms และโปรแกรมนี้ 100.30 ms
